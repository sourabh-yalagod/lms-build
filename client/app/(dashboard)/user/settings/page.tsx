import Settings from '@/components/Settings';
import React from 'react';

const UserSettings = () => {
  return (
    <Settings
      title={'User Settings'}
      subTitles={'manage user the user related setting accordingly'}
    />
  );
};

export default UserSettings;
